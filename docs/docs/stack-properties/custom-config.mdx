# Custom Config

Each custom stack implementation supports its own configuration, which is defined using the `customConfig` property.

:::note
Documentation will be added soon
:::

## Usage In Configuration

`customConfig` property can be defined only in custom stack configuration files.
