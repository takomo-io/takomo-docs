# Custom Type

You can mark a stack configuration as a custom stack by defining the `customType` property.
You can find more information about custom stacks [here](../custom-stacks/introduction.mdx).

#### Examples

Setting the `customType` property:

```yaml
customType: my-custom-stack
```

## Usage In Configuration

`customType` property can be defined only in custom stack configuration files.
